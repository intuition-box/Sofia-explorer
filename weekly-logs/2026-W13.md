# Weekly Log — Week 13 (March 21–27, 2026)

> Responsive redesign, feed semantics overhaul, deployment infrastructure, and performance fixes.

## Responsive Layout (Phase 1–2)
- **Sidebar drawer mode** — Left sidebar becomes an overlay with backdrop on < 1280px, hamburger toggle in header
- **Profile drawer** — Hidden on mobile, accessible via panel button in header on profile pages
- **Cart drawer** — Full-width on mobile, adjusted top position for compact header
- **Responsive grids** — Replaced all fixed  grids across 8 grid components (stats, trending, platforms, claims, categories, last activity, top claims, public stats)
- **Search bar** — Only visible on `/feed` and `/profile*` pages

## Feed Semantics
- **"claims" phrasing** — Changed from "visits for work" to "claims [title] is for work" for all intention types
- **Trust/Distrust** — Kept original verb format ("trusted [title]")
- **"is following"** — Added follow predicate mapping with direct verb display

## Platform Market
- **Connect buttons disabled** — All platform connect buttons grayed out with "Coming soon" tooltip
- **Info banner** — Added explanation banner directing users to Certify via extension
- **Topic limit** — Max 3 interests simultaneously with visual feedback 

## Favicon Optimization
- **Local favicon helper** —  maps domains to local via platform catalog, falls back to Google API only for unknown domains
- **Migrated 5 services** — feedProcessing, trendingService, domainTrendingService, topicCertificationsService, TopClaimsSection

## ENS Resolution Rewrite
- **Removed cascade** — Eliminated cloudflare-eth RPC, TheGraph ENS subgraph, and on-chain reverse lookup (all had CORS issues)
- **Single source** — GraphQL Intuition for indexed labels for ENS reverse lookup (no CORS issues)
- **No retry spam** — Addresses resolved  are cached permanently, not retried

## Deployment (Coolify/Hetzner)
- **Nginx SPA config** — Fixed 404 on direct route access
- **Build pack** — Settled on Nixpacks with custom Nginx configuration
- **CORS investigation** — Identified intermittent CORS 

## Resilience
- **Fetch retry helper** — exponential backoff (1s → 2s → 4s), applied to 4 critical hooks
- **Cart phantom fix** —  returns false until positions are successfully fetched, preventing already on-chain topics from being re-added to cart
- **React Query config** — Increased to 3 retries with exponential backoff globally

